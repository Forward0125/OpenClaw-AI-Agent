import { Card } from "@/components/Card";

export default function Page() {
  return (
    <Card title="Long-term Memory">
      <div className="p-6 text-sm text-muted">
        Keyword recall over past workflows + fixtures &mdash; coming step 3.
      </div>
    </Card>
  );
}
