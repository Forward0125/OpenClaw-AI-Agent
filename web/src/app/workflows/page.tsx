import { Card } from "@/components/Card";

export default function Page() {
  return (
    <Card title="Workflow Center">
      <div className="p-6 text-sm text-muted">
        DAG execution + activity log &mdash; coming steps 8&ndash;10.
      </div>
    </Card>
  );
}
